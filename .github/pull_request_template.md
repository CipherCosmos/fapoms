## What changed

<!-- One or two sentences. Link the plan/phase item if this is part of the reorganization. -->

## How verified

<!-- Commands run and their result, e.g. `npm run typecheck`, `npm run lint`, relevant test suites. -->

- [ ] `npm run -s typecheck`
- [ ] `npm run -s lint`
- [ ] `npm test --workspaces --if-present`

## `npm run reorg:verify` result

<!-- Paste the summary output, or write "n/a" if this PR predates the reorganization tooling. -->

## Deploy impact

<!-- Does this touch anything auto-deploy.sh picks up (compose files, Dockerfiles, deploy/*.sh,
     scripts/*.mjs)? Does it add a migration? Any env var changes needed on a server? -->

## Screenshots

<!-- Required for any UI change. Before/after if the change is visual. -->
